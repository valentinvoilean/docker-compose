# Optional in stack env: UPTIME_KUMA_VERSION (default: latest)
services:
  uptime-kuma:
    image: louislam/uptime-kuma:${UPTIME_KUMA_VERSION:-latest}
    container_name: uptime-kuma
    restart: always
    ports:
      - "3001:3001"
    volumes:
      - /share/Container/uptime-kuma:/app/data
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - proxy-net

networks:
  proxy-net:
    external: true